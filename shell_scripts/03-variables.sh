#!/bin/bash

# VAR=DATA
# variable a name which holds some data

NAME=Raju

echo Hello $NAME
echo Good Morning $NAME
echo See you $NAME 

# Shell Variables by default , does not have data types. Default data type is string.
# Variable Name can consist of 
	# a-z, A-Z, 0-9, _
	# Variable should start with number
		# 1name -> wrong syntax
